project_types:
- name: api-service-generator
  description: Project for Models describing the components which an enterprise system consists of.
  base_type_name: generator
  plugins:
  - group: laplacian-arch
    name: datasource.domain-model-plugin
    version: '1.0.0'
  - group: laplacian-arch
    name: api-service.domain-model-plugin
    version: '1.0.0'
  models:
  - group: laplacian-arch
    name: project-types
    version: '1.0.0'
  templates:
  - group: laplacian-arch
    name: datasource.flyway-migration-template
    version: '1.0.0'
  - group: laplacian-arch
    name: api-service.springboot2-template
    version: '1.0.0'

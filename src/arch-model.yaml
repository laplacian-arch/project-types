project_types:
- name: arch-model
  description: Project for Models describing the components which an enterprise system consists of.
  base_type_name: model
  plugins:
  - group: laplacian-arch
    name: datasource.domain-model-plugin
    version: '1.0.0'
  - group: laplacian-arch
    name: service-api.domain-model-plugin
    version: '1.0.0'
  models:
  - group: laplacian-arch
    name: datasource.domain-model
    version: '1.0.0'
  - group: laplacian-arch
    name: service-api.domain-model
    version: '1.0.0'
  templates:
  - group: laplacian-arch
    name: arch-model.project-template
    version: '1.0.0'
